import { FunctionComponent } from 'react';

import classes from '@/components/Footer/Footer.module.scss';

export const Footer: FunctionComponent = () => {
  return <footer className={classes.footerWrapper} />;
};
