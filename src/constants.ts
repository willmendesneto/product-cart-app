const LOCALSTORAGE_PREFIX = 'navaliaApp.';

export const CART_LOCAL_STORAGE = `${LOCALSTORAGE_PREFIX}cart`;
export const USE_GET_PRODUCTS = 'useGetProducts';

// For more details, please check `jsonServer()` configuration, as described on `vite.config.ts` file
export const API_URL = `${window.location.origin}/api`;
